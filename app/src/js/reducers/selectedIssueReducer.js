import * as types from '../actions/actionTypes';
import initialState from './initialState';

const selectedIssue = (state = initialState.selectedIssue, action) => {
  switch (action.type) {
  }
};

export default selectedIssue;
